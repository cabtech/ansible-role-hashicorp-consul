[Unit]
Description=Consul client process
After=network.target
 
[Service]
Type=simple
ExecStart=/bin/bash -c '{{hashicorp_dir_bin}}/consul agent -config-file {{consul_etc_dir}}/client.json -bind={{consul_client_addr}}'
TimeoutStartSec=0
 
[Install]
WantedBy=default.target
