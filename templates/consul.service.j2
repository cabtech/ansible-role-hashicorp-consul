[Unit]
Description=Consul Startup process
After=network.target
 
[Service]
Type=simple
ExecStart=/bin/bash -c '{{hashicorp_dir_bin}}/consul agent -config-dir {{consul_etc_dir}}/'
TimeoutStartSec=0
 
[Install]
WantedBy=default.target
